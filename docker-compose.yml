version: '3.8'

services:
  led-loop:
    image: bbblq/led-loop:latest
    container_name: led-loop
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=3000
      - NODE_ENV=production
